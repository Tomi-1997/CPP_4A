#if !defined PLAYER_H
#define PLAYER_H

#include <string>
#include <vector>
#include "Game.hpp"

namespace coup
{
    class Player
    {
        public:
        Player();
        ~Player();
    };
}
#endif